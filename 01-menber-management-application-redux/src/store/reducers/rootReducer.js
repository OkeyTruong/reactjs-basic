import React from 'react'

const initialState = {
    members:[
        {id:1, name:"Huy"},
        {id:2, name:"Okey"},
        {id:3, name:"lh222k"},
    ]
}
const rootReducer = (state = initialState , action) => {
    return state
}

export default rootReducer